# گزارش خروجی — تولید مجموعه وظایف و منابع مشترک

این گزارش خروجی دستور زیر را توضیح می‌دهد و نشان می‌دهد هر بخش آن با کدام
بند از تعریف پروژه مطابقت دارد.

```bash
python -m msrp_project.main
```

---

## ۱. سطر تنظیمات

```
m=8 cores, U_norm=0.5, U=4.00, n=4 tasks, n_r=4, CSP=0.5, seed=1
```

تنظیمات اولیه‌ی سامانه که پیش از تولید مشخص شده‌اند:

| نماد | مقدار | معنی |
|---|---|---|
| `m` | 8 | تعداد پردازنده‌های سامانه محلی |
| `U_norm` | 0.5 | بهره‌وری به‌هنجارشده (بار هر پردازنده) |
| `U` | 4.00 | بهره‌وری کل، از رابطه `U = m × U_norm` |
| `n` | 4 | تعداد گراف‌های وظیفه |
| `n_r` | 4 | تعداد منابع مشترک |
| `CSP` | 0.5 | سهم بخش بحرانی از زمان اجرای هر گره |
| `seed` | 1 | دانه‌ی تصادفی، برای تکرارپذیری نتایج |

**تطابق با تعریف پروژه:** رابطه‌ی `U = m·U_norm` عیناً پیاده‌سازی شده و
`U_norm` در بازه‌ی مجاز `[0.1, 1]` قرار دارد. مقدار `U` به‌صورت مشتق‌شده
محاسبه می‌شود، بنابراین هرگز با `m` و `U_norm` ناسازگار نمی‌شود.

---

## ۲. خلاصه‌ی کل سامانه

```
4 tasks, 4 resources, 8 cores, 169 nodes, 120 critical sections
U = 4.000   U_norm = 0.500
```

- در مجموع **۱۶۹ گره** در چهار گراف وظیفه تولید شده است (36 + 37 + 45 + 51).
- در مجموع **۱۲۰ بخش بحرانی** ساخته شده است.
- بهره‌وری کل تولیدشده دقیقاً برابر `4.000` است، یعنی همان `8 × 0.5`.

**نکته‌ی مهم برای صحت‌سنجی:** مجموع بهره‌وری چهار وظیفه برابر است با

```
0.0495 + 3.6255 + 0.2471 + 0.0778 = 3.9999 ≈ 4.00
```

یعنی الگوریتم **RandFixedSum** بهره‌وری کل را دقیقاً بین وظایف تقسیم کرده است.

---

## ۳. مشخصات هر گراف وظیفه

نمونه‌ی وظیفه‌ی اول:

```
task 1   T=4000   D=4000   u=0.0495
  C (total execution time) = 198.04
  L (critical path length) = 56.87
  graph: 36 nodes, 77 edges
  resources: R1, R2, R3, R4   (R1:8, R2:10, R3:9, R4:10)
```

| مقدار | معنی | تطابق با تعریف پروژه |
|---|---|---|
| `T=4000` | دوره تناوب | از مجموعه‌ی `{2000, 4000, 6000}` انتخاب شده |
| `D=4000` | مهلت نسبی | `D_i = T_i` (مهلت ضمنی) |
| `u=0.0495` | بهره‌وری وظیفه | سهم این وظیفه از `U` |
| `C=198.04` | بدترین زمان اجرای کل | `C_i = Σ c_{v_i,k}` |
| `L=56.87` | طول مسیر بحرانی | طولانی‌ترین مسیر گراف |
| `36 nodes` | تعداد گره‌ها | ۳۴ گره اصلی + یک گره source + یک گره sink |
| `R1:8, R2:10, ...` | تعداد دسترسی به هر منبع | توزیع دسترسی‌ها |

**راستی‌آزمایی رابطه‌ی زمان اجرا:** برای هر چهار وظیفه، رابطه‌ی
`C = u × T` برقرار است:

| وظیفه | u | T | u × T | C در خروجی |
|---|---|---|---|---|
| ۱ | 0.0495 | 4000 | 198.00 | 198.04 |
| ۲ | 3.6255 | 4000 | 14502.00 | 14502.16 |
| ۳ | 0.2471 | 2000 | 494.20 | 494.28 |
| ۴ | 0.0778 | 4000 | 311.20 | 311.23 |

**نکته‌ی قابل توجه:** بهره‌وری وظیفه‌ی ۲ برابر `3.6255` و **بزرگ‌تر از ۱**
است. این دقیقاً مطابق تعریف پروژه است که تصریح می‌کند «بهره‌وری هر وظیفه
می‌تواند عددی بزرگ‌تر یا کوچک‌تر از ۱ باشد» — چون یک گراف وظیفه می‌تواند
هم‌زمان چند پردازنده را مشغول کند. در مقابل، بهره‌وری **هر گره** همواره
کوچک‌تر از ۱ است (ستون `u` در سطرهای گره‌ها؛ بزرگ‌ترین مقدار در این خروجی
`0.3618` است).

همچنین در همه‌ی وظایف `L ≤ C` برقرار است، که طبیعی است: `C` مجموع کل کار و
`L` طولانی‌ترین زنجیره‌ی وابسته است.

---

## ۴. توالی اجرای هر گره — مهم‌ترین بخش خروجی

این بخش، پیاده‌سازی «تخصیص منابع به هر وظیفه» را نشان می‌دهد.

```
node 12  wcet=    6.85  u=0.0017  cs=3
        NS 0.47 | CS R4 1.57 | NS 0.44 | CS R1 0.60 | NS 0.16 | CS R2 1.26 | NS 2.35
```

خوانش این سطر:

- گره شماره ۱۲، با زمان اجرای `6.85` و بهره‌وری `0.0017`
- `cs=3` یعنی این گره سه بخش بحرانی دارد
- توالی اجرا: `0.47` واحد اجرای عادی → قفل‌کردن منبع `R4` به‌مدت `1.57` →
  `0.44` واحد عادی → قفل `R1` به‌مدت `0.60` → `0.16` عادی → قفل `R2` به‌مدت
  `1.26` → و در پایان `2.35` واحد عادی

که در آن `NS` بخش عادی (normal section) و `CS Rq` بخش بحرانی روی منبع `Rq`
است.

### تطابق این ساختار با تعریف پروژه

**۱. ساختار متناوب.** تعریف پروژه هر گره را به‌صورت
`ν = {c_{i,1}, c'_{i,1}, …, c'_{i,s-1}, c_{i,s}}` توصیف می‌کند و می‌گوید
«هر بخش بحرانی بین دو بخش عادی قرار می‌گیرد». در خروجی بالا با ۳ بخش بحرانی،
دقیقاً ۴ بخش عادی وجود دارد و توالی همیشه با بخش عادی شروع و تمام می‌شود.

**۲. سهم CSP.** مجموع طول بخش‌های بحرانی این گره:

```
1.57 + 0.60 + 1.26 = 3.43   و   3.43 / 6.85 = 0.50
```

یعنی دقیقاً `CSP = 0.5` از زمان اجرای گره. این نسبت برای همه‌ی گره‌ها برقرار
است؛ مثلاً گره ۱ از وظیفه ۱: `2.08 / 4.16 = 0.50`. تقسیم این سهم بین
بخش‌های بحرانی به‌صورت تصادفی و با الگوریتم **RandFixedSum** انجام شده و
باقی‌مانده‌ی زمان، بخش عادی محسوب می‌شود.

**۳. دسترسی غیرتودرتو (non-nested).** چون بخش‌ها به‌صورت ترتیبی اجرا می‌شوند
و همیشه یک بخش عادی بین دو بخش بحرانی قرار دارد، هیچ گرهی نمی‌تواند پیش از
آزادکردن یک منبع، منبع دیگری را بگیرد. این شرط در خروجی قابل مشاهده است:
هرگز دو `CS` پشت سر هم نمی‌آید.

**۴. گره‌های بدون منبع.** گره‌هایی مانند

```
node 2   wcet=    9.33  u=0.0023  cs=0
        NS 9.33
```

هیچ منبعی نمی‌گیرند و کل زمانشان بخش عادی است.

**۵. گره‌های source و sink.** گره ۰ و آخرین گره هر گراف بدون زمان اجرا
هستند، مطابق تعریف پروژه که زمان اجرای این دو گره را صفر تعیین کرده است.

---

## ۵. جدول منابع مشترک

```
R1  accesses=30   [task1:8, task2:9, task3:9, task4:4]
R2  accesses=30   [task1:10, task2:6, task3:7, task4:7]
R3  accesses=30   [task1:9, task2:4, task3:6, task4:11]
R4  accesses=30   [task1:10, task2:9, task3:5, task4:6]
```

برای هر منبع، **تعداد کل دسترسی‌ها** از پیش تعیین شده (اینجا ۳۰) و سپس این
دسترسی‌ها به‌صورت تصادفی بین گراف‌های وظیفه توزیع شده است. جمع هر سطر دقیقاً
۳۰ است:

```
R1:  8 + 9 + 9 + 4  = 30
R2: 10 + 6 + 7 + 7  = 30
R3:  9 + 4 + 6 + 11 = 30
R4: 10 + 9 + 5 + 6  = 30
```

مجموع کل: `4 × 30 = 120` دسترسی، که دقیقاً با «۱۲۰ بخش بحرانی» در سطر خلاصه
مطابقت دارد — یعنی هر دسترسی واقعاً به یک بخش بحرانی در یک گره تبدیل شده است.

**تطابق با تعریف پروژه:** این همان بندی است که می‌گوید تعداد کل دسترسی به هر
منبع توسط تمام گراف وظایف از پیش انتخاب می‌شود و سپس بین وظایف مختلف توزیع
می‌گردد. توزیع نامتوازن است (مثلاً `R3` برای وظیفه ۴ یازده بار و برای وظیفه ۲
چهار بار)، که همان رفتار تصادفی مورد نظر تعریف پروژه است.

---

## ۶. هسته‌ها

```
cores (8) - created, no tasks assigned yet (that is the next stage)
```

هشت هسته ساخته شده‌اند، اما هنوز وظیفه‌ای به آن‌ها تخصیص داده نشده است، چون
پارتیشن‌بندی و زمان‌بندی مربوط به مرحله‌ی بعدی است.

---

## ۷. جمع‌بندی تطابق با تعریف پروژه

| بند تعریف پروژه | وضعیت | شاهد در خروجی |
|---|---|---|
| `U = m × U_norm` | ✅ | `U=4.00` از `8 × 0.5` |
| توزیع `U` بین وظایف با RandFixedSum | ✅ | مجموع بهره‌وری‌ها = 3.9999 |
| بهره‌وری وظیفه می‌تواند از ۱ بزرگ‌تر باشد | ✅ | وظیفه ۲ با `u=3.6255` |
| گراف Erdős–Rényi با `p = 0.1` | ✅ | مثلاً ۳۶ گره و ۷۷ یال |
| افزودن گره source و sink با زمان صفر | ✅ | `node 0 (source)` و `node 35 (sink)` |
| تعداد گره‌ها در بازه `[20, 50]` | ✅ | ۳۴، ۳۵، ۴۳ و ۴۹ گره اصلی |
| بهره‌وری هر گره کوچک‌تر از ۱ با UUniFast | ✅ | بیشترین مقدار `0.3618` |
| دوره تناوب از `{2000, 4000, 6000}` | ✅ | `T=4000` و `T=2000` |
| `c = u × T` | ✅ | جدول بخش ۳ |
| `C_i = Σ c` | ✅ | `C=198.04` برای وظیفه ۱ |
| `L_i` طولانی‌ترین مسیر | ✅ | `L=56.87` |
| `D_i = T_i` | ✅ | `T=4000  D=4000` |
| تعداد منابع در بازه `[2, 8]` | ✅ | `n_r=4` |
| تعداد کل دسترسی هر منبع، توزیع‌شده بین وظایف | ✅ | جدول بخش ۵ |
| ساختار متناوب بخش عادی/بحرانی | ✅ | سطرهای `NS ... CS ... NS` |
| سهم `CSP` از زمان گره با RandFixedSum | ✅ | `3.43 / 6.85 = 0.50` |
| دسترسی غیرتودرتو | ✅ | هرگز دو `CS` پشت سر هم |

---

## ۸. بخش‌های باقی‌مانده

مطابق ترتیبی که مشخص شده است (تولید وظایف ← پروتکل و اجرا ← واگذاری)، این
موارد در مراحل بعدی انجام می‌شوند:

1. **تخصیص وظایف به هسته‌ها** (مثلاً با الگوریتم WFD)
2. **زمان‌بند**: محاسبه‌ی هایپرپریود، پیشروی گام‌به‌گام زمان، و انتخاب وظیفه
   با نزدیک‌ترین مهلت (EDF)
3. **پروتکل مدیریت منبع مشترک**: spin-lock، suspension-based و تصمیم‌گیری
   بین این دو
4. **واگذاری به سرورهای لبه** و سربار ارتباطی مربوط به آن (`CCR`)

نمودارهای زمان‌بندی‌پذیری و کیفیت خدمات نیز پس از افزوده‌شدن زمان‌بند قابل
تولید خواهند بود، چون سنجش آن‌ها نیازمند اجرای واقعی مجموعه وظایف است. زیرساخت
تولید ۱۰۰ مجموعه وظیفه به‌ازای هر تنظیم هم‌اکنون آماده است.
